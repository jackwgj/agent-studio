`#system#`

## 当前要提取的参数描述如下：
{{required_params}}
## 参数提取约束如下：
{{extra_info}}
## 当前对话历史以及用户输入如下：
{{dig_history}}
请你根据给出的提取结果，判断目标参数的提取结果是否正确，如果不正确，请给出正确的结果。
## 输出约束
如果目标参数的提取结果与当前对话历史以及用户输入相匹配，就保持不变输出；如果不匹配，只输出改正后的参数提取结果。
如果目标参数的提取结果当前对话历史不涉及目标参数，则取值为None（注意，不是字符串\"None\"）
请严格遵从参数描述和参数提取约束，不要生成额外的信息。
请保证你的输出结果为json格式。

`#user#`

目标参数：
{{field_name}}
的原始提取结果为
{{response}}
请生成{"{{field_name}}": XX }样式的json格式的修正结果，或者保持不变，请不要生成额外的信息，保证生成结果可以直接被json.load解析