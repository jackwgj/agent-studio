# 任务描述

请帮助我提升分类器的分类准确率。以下是分类器的总体描述及各个类别的详细信息。请根据这些信息执行以下步骤：

## 1. 分析类别

识别可能分类准确率较低的类别，并优先处理这些类别。在这一部分，请输出推理的思维链，以帮助理解判断过程，确保格式清晰，以便后续的 `<START>` 和 `<END>` 解析不受影响。

## 2. 生成样本

为需要增强的类别生成**总共**`{{generation_num}}`个输入输出对。**请确保生成的样本数量恰好为此数目**，并尽量覆盖所有类别，确保每个类别都有对应的样例生成。特别是在类别数量少于生成数量时，需兼顾各分类的难度。**对于描述中包含多个意图的类别，应为每个意图生成对应的示例，并重点生成这些类别的样本**。样本中应包含**大约一半的多轮对话**。

## 输入格式：

- **分类器整体描述**：`{{prompt}}`
- **类别描述信息**：
  - `{{branches}}`（包含每个类别的 `id` 和描述）
- **生成样本的数量**：`{{generation_num}}`

## 输出格式：

请提供**恰好**`{{generation_num}}`个生成的输入输出对，格式如下：

```
<START>
input: [用户和助手之间的对话历史，格式可以是：
   - 单轮对话：如“用户：XXX”
   - 多轮对话：如“用户：XXX 助手：XXX 用户：XXX”
   请确保生成的对话历史多样化，包括单轮和多轮的组合。注意：如果是多轮对话，最后一轮必须是“用户：XXX”，因为我们要基于用户的输入来做分类。]
output: {"class": "分类{num}", "reason": "当前输入xxxx"}
<END>
```

## 注意事项

1. **生成的样本数量必须恰好为`{{generation_num}}`个，而不是每个类别都生成此数量**。
2. 每个输入输出对必须用 `<START>` 和 `<END>` 包裹。
3. `input` 的内容应支持单轮或多轮对话历史，鼓励使用多样化的对话格式：
   - **单轮对话示例**：`用户：你好`
   - **多轮对话示例**：`用户：你好 助手：你好！有什么我可以帮助你的吗？ 用户：我想了解一下天气。`
   特别注意：在多轮对话中，最后一轮必须是“用户：XXX”，以确保分类基于用户的最新输入。**建议生成的样例中大约一半为多轮对话**。
4. 生成的 `class` 必须是“分类{num}”的格式，其中数字num应与 `branches` 中 `id` 的数字相对应，比如，如果对应id是 `branch_2`，则需要输出“分类2”。
5. 在推理过程中，首要关注用户当前的输入，仅在无法确定类别时才结合历史信息进行判断，以防历史信息误导。
6. 生成的样本应尽可能覆盖所有类别，确保每个类别都有对应的样例，特别是在类别数量少于生成数量时，需兼顾各分类的难度。
7. 对于描述中包含多个意图的类别，应为每个意图生成对应的示例，并确保这些类别的样本数量充足，以增强分类器性能。
8. 请确保生成样本具有代表性和多样性，以增强分类器性能。
9. 必须严格按照给定格式生成样例。