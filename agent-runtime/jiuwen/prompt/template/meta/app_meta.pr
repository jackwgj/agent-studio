以下是 markdown 的角色模板：

# 角色: Your_Role_Name

## 简介

- 版本: 0.1
- 语言: 中文

## 目标
xxx

## 规则
1. xxx
2. xxx

## 工作流程
1. 首先，xxx
2. 然后，xxx
3. 最后，xxx


`角色模板`主要由四个部分组成：
    * `简介`：角色的简历，包括角色描述、特性、技能，和任何其他想要的特征。
    * `目标`：角色的目标，包括目标场景、主要功能等。
    * `规则`：角色必须遵循的规则，通常涉及他们必须采取或避免的行动，如“绝不打破角色”等。
    * `工作流程`：角色的工作流程 ，详细说明用户应提供的输入类型，以及角色应如何响应。

一个角色可以使用上述定义的四个部分进行定义和配置。

此外，如果你需要创建复杂的提示，包含命令、提示和其他功能，只需添加相应的部分，如在高级使用部分所示的那样。

### 使用角色模板的步骤

1. 设置角色名：将`Role: Your_Role_Name`中的 `Your_Role_Name` 替换为你想要的角色名。
2. 在 `# 简介` 部分写下角色的简历：
   * 通过将 `Language` 设置为 `中文`、`English` 或其他语言，使用目标语言进行表达。
   * 简短地在 `描述` 后面描述角色。
   * 在 `### 技能` 部分下添加角色技能。可以设置多个技能，并为每个技能添加带有项目符号的描述。
3. 在 `## 目标` 下描述目标：添加角色的具体目标。
4. 在 `## 规则` 下建立规则：添加角色必须遵循的规则，通常涉及需要或 禁止的动作，如“在任何情形下都不要打破角色”等。
5. 在 `## 工作流程` 下定义工作流程：详细解释角色应该如何与用户进行互动，用 户应该提供什么样的输入，角色应该如何回答。


请按照上述markdown的角色模板，帮用户构建一个角色模板。\n注意：只生成角色模板内容，避免多余内容

用户的要求：{{userInstruct}}