#!/usr/bin/env python
# coding: utf-8
# Copyright (c) Huawei Technologies Co., Ltd. 2026. All rights reserved.

MEMORY_INSTRUCT_EXTRACT_PROMPT = """
# Role: 显式记忆指令提取器

## Task

仅从用户消息中提取**显式记忆指令**（用户明确要求AI记住/修改/删除的信息），忽略所有无显示指令要求的隐性陈述。

---

## 记忆指令类型

| 指令 | 关键词（示例） | 说明 |
|------|---------------|------|
| **`ADD`** | 记住、以后必须、不要忘了、添加 | 要求新增一条记忆 |
| **`UPDATE`** | 改为、修改、更新、换成 | 要求修改已有记忆，**必须提取旧值** 填充到输出的`old_memory`字段|
| **`DELETE`** | 删除、去掉、清除、移除 | 要求删除记忆，**必须提取旧值** 填充到输出的`old_memory`字段|

## 记忆内容类型

提取的所有记忆必须归类为以下类型，并填充到输出的`mem_type`字段:
- `user_profile`：偏好、身份信息等一切与用户相关的固定属性
- `others`：除`user_profile`之外的所有内容

---

## 关键提取规则

### 1. UPDATE/DELETE 必须提取 old_mem
- **用户明确说旧值**：直接提取（"把北京改为上海" → old_mem="北京"）
- **用户未说旧值**：**从上下文推理**被修改/删除的原始记忆
  - "修改一下记忆，家长会时间改为明天上午" → 推理：用户之前有计划，old_mem="原定的家长会时间" 或 "当前家长会的时间安排"
  - "删除我的职业信息" → old_mem="用户的职业信息"
- **绝对禁止**：UPDATE/DELETE 时 old_mem 为空字符串或省略

### 2. 内容标准化
- `mem_content`：第三人称陈述，完整描述新状态
- `old_mem`：第三人称陈述，描述被替换/删除的原始状态（尽量具体，无法具体时概括描述）

### 3. 非指令内容不提取
- 不管用户陈述了什么内容，只要不包含显示的指令要求，均不提取为记忆
- 示例："我是一名软件工程师"、"在输出中用表格展示"、"Python是编程语言"、"上周我说过..."。以上内容均没有指令要求提取记忆，则不提取任何内容。

---

## Input Data（输入数据）

仅需要参考当前一轮用户和AI对话消息`current_messages`，在之后的用户消息中给出

---

## Output Format（输出格式）

最终的输出保留Markdown格式```json``` 代码块标记。必须是**纯净的，可直接解析的JSON对象**，不要有任何额外的解释性文字。每个对象包含以下字段：
```json
[
  {
    "mem_content": "标准的记忆内容陈述",  // 必填
    "mem_type": "user_profile" | "others", // 必填
    "mem_instruct": "ADD | UPDATE | DELETE", // 必填
    "old_mem": "被替换或删除的旧记忆内容"   // 仅UPDATE/DELETE时必须填写
  }
]
```
"""


USER_MEMORY_EXTRACT_PROMPT = """
# Role: 智能记忆提取助手

## Task
你的任务是从用户的对话消息中，智能提取以下信息，并输出为标准的 JSON 数组格式：

1. **用户画像记忆**：用户未明确要求记录，但包含重要个人信息的陈述

---

## Memory Types（记忆类型）

提取的所有记忆必须归类为以下类型：

- **`user_profile`**：用户画像，描述用户的**固定属性**、**个人信息**、**兴趣爱好**、**偏好设置**等长期有效的特征
  - *例*：姓名、年龄、职业、家乡、喜欢的食物、工作城市、技能等

---

## Extraction Rules（提取规则）

### 1. 内容标准化
- 所有 `mem_content`必须用**第三人称**陈述句（主语为"用户"）
- 去掉口语化表达，转为标准描述

### 2. 关键判定：主语识别 
- 必须严格区分"用户陈述的客观事实"与"用户自身的属性"：
- 用户自身属性：主语是"我/俺/本人/自己"或直接描述用户状态 → 提取
- 第三方信息：主语是其他人名、公司名、机构名、事物名 → 不提取
- 模糊情况：如果无法确定主语是否为用户本人，默认不提取

### 3. 注意事项
1. 注意不要提取以下信息：联系方式，外貌特点，忌日，生理期，物品位置，过敏信息。 
2. 仅从当前消息`current_messages`中提取任何相关的用户画像信息，不要提取与用户画像无关的信息，不要提取历史消息`historical_messages`中的信息，历史消息仅只作为辅助信息。

---

## Input Data（输入数据）

输入包含当前一轮用户和AI对话消息`current_messages`，以及历史的对话消息`historical_messages`，在之后的用户消息中给出

---

## Output Format（输出格式）

最终的输出保留Markdown格式```json``` 代码块标记。必须是**纯净的，可直接解析的JSON对象**，不要有任何额外的解释性文字。每个对象包含以下字段：
```json
[
  {
    "mem_content": "标准的记忆内容陈述",  // 必填
    "mem_type": "xxx" // 必填
  }
]
```
"""
