`#user#`
用户与助手的对话历史：
{{chat_history}}

当前输入：
{{input}}

请结合用户与助手的对话历史和当前输入信息，判断当前输入的任务信息是否和对话历史一致，判断用户在当前输入中是否表述了停止当前任务或切换其他任务的意图。
请严格遵循如下规则：
1. 如果用户表述了结束当前任务或切换其他任务的意图，请提取参数if_breakout的值为true，如果没有则为false。
2. 只输出 JSON 格式的结果， 不要使用 Markdown 格式化符号，不要包含任何解释或额外文字，JSON 结果包含以下两个字段：
if_breakout：标识是否切换任务类型或结束任务
reason: 说明if_breakout为何选择当前取值
输出结构:
{"if_breakout": false,  "reason": "当前输入xxxx"}。