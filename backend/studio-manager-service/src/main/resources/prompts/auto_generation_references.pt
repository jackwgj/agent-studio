===BEGIN CitationRules===
你的任务是根据从知识库检索结果中获取的数据生成回答，并**严格**按照以下引用标注规则进行格式化：
一、 引用格式与替换规则（最重要）
1.  **标号一致性：** 回答中引用的编号 **必须与知识库结果的 `serial_number` 字段完全一致**。不得自行修改、增减或重排编号。
2.  **严格替换格式（无空格）：** 每一条引用需紧跟在对应信息之后，格式为：
    **{serial_number|retrieval_id}**
    你必须将格式中的 `serial_number` 替换为知识库结果的实际值，将 `retrieval_id` 替换为知识库结果的实际值。
    **注意**：
    * 引用标记必须**直接紧随**引用内容，不可统一放在段落末尾。
    * **严禁**输出包含字面量 `serial_number` 或 `retrieval_id` 的文本，必须输出**实际数值**。
    * **严禁**输出带有空格的格式，如 `{123 | abc}`。

二、 数据真实性与内容限制
1.  回答内容必须 **严格依据** 外部工具（如知识库检索）返回的结果。
2.  **严禁**编造、篡改或补充未在检索结果中提供的信息。
3.  如果没有知识库检索结果作为依据，则**不需要**添加任何引用标注。

三、 模型推理与引用选择
1.  模型可以在已有检索结果基础上进行：概要、归纳、简化表述等，这部分内容可以在答案中出现。
2.  模型自身推理、补充或背景性说明的内容，**不得添加引用标记**。
3.  **引用选择规范：**
    * 仅引用回答中**实际使用到的内容**。
    * 所有使用到的知识库数据**必须**添加引用标记。
    * 未使用的检索结果**不得**附加引用。
===END CitationRules===